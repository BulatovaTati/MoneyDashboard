🧩 Компонент: HomeTab
Отримує транзакції при завантаженні (getTransactions()).
Виводить:

<Balance /> — лише на мобільних  використовуємо кастомний хук useMedia.

<TransactionsList /> — список транзакцій.

<AddButton /> — кнопка для додавання.


🧩 Компонент: StatisticsTab

Виводить сторінку статистики з двома основними блоками:

Графік (<Chart />) з заголовком “Statistics”.

Дашборд і таблиця (<StatisticsDashboard /> та <StatisticsTable />).


💱 Компонент: CurrencyTab 
Структура рендеру:
CurrencyTab 👉 рендерить компонент Currency

Currency 👉 показує таблицю валют (USD / EUR) та графік

В середині використовує CurrencyChart для візуалізації курсів купівлі

🛠 Що потрібно:
Отримати курси валют з API (getCurrencyRates)

Вивести таблицю з курсами купівлі/продажу

Побудувати графік (CurrencyChart) лише по купівлі

📄 DashboardPage

🔁 Логіка:
На mount:

диспатчить getTransactions()

диспатчить getTransactionsCategories()

🧩 Компонент містить:
Header

Navigation

(Balance та Currency) - означає, що Balance та Currency рендеряться тільки на пристроях, які не є мобільними, 
тобто: від планшета і вище (tablet+, desktop) використовуємо кастомний хук useMedia.

Модальні вікна:

ModalLogOut

ModalEditTransaction

ModalAddTransaction

Toaster (для сповіщень)



ModalWrapper  - обгортка для модального вікна, яка відповідає за рендеринг самого модального вікна, його стилізацію, закриття, а також адаптивність для мобільних пристроїв.
 Що приймає:
children — вміст модального вікна (будь-який JSX, який ти передаєш).

isOpenModal — булеве значення, яке вказує, чи має модалка бути відкрита (true) чи закрита (false).

 Приклад використання:

<ModalWrapper isOpenModal={modalIsOpen}>
  <p>Hello, I'm inside the modal!</p>
</ModalWrapper>

ModalAddTransaction — це компонент, який відкриває модальне вікно для додавання транзакції, коли стан isAddModalOpen у Redux встановлений на true.

Він використовує:
ModalWrapper для показу модалки.
AddTransactionForm всередині модалки для введення даних транзакції.

ModalEditTransaction — компонент, який відкриває модальне вікно для редагування транзакції, коли стан isEditModalOpen у Redux встановлений на true.

Він використовує:
ModalWrapper для відображення модального вікна.
EditTransactionForm всередині модалки для редагування транзакції.


ModalLogOut — компонент, який відображає модальне вікно для підтвердження виходу з системи.

Основні функції:

ModalWrapper — обгортає модалку.
Icons — відображає іконку логотипу для десктопної версії.
logoutThunk — викликає операцію для виведення користувача з системи.
closeModal — закриває модалку при натисканні "Cancel".
Кнопки:
"logout" — викликає функцію для виходу.
"cancel" — закриває модалку без дій.
Модалка відображається, коли стан isLogOutModalOpen у Redux встановлений на true.


Компонент Icons відповідає за відображення іконок за допомогою SVG.

Основні моменти:
className — клас для стилізації іконки.
width та height — розміри іконки, за замовчуванням 12px на 12px.
name — назва іконки, що використовується в атрибуті href для отримання відповідної іконки з файлу icons.svg.
Використання: Компонент рендерить SVG з конкретним name, що вказує на необхідну іконку з icons.svg, за допомогою елементу <use>.
Це дозволяє повторно використовувати одну іконку у кількох місцях без дублювання даних.

Компонент Navigation відповідає за рендеринг навігаційного меню з посиланнями, що використовують компонент NavLink з react-router-dom для керування маршрутизацією.

Ось що робить цей компонент:

useMedia: Використовується для перевірки, чи є пристрій мобільним (якщо так, то isMobile буде true).
clsx: Використовується для динамічного додавання класів до елементів, наприклад, коли посилання активно, додається клас s.linkActive.
Icons: Компонент для відображення SVG-іконок, який приймає ім'я іконки, ширину та висоту.
NavLink: Використовується для навігації між різними маршрутами в додатку. Всі посилання, коли активні, отримують клас s.linkActive.

Мобільний/десктопний вигляд:
Для мобільних пристроїв, меню включає тільки один додатковий елемент — посилання на "/currency".
Для планшетної та десктопної версії додаються текстові підписи до кожної іконки ("Home", "Statistics").
Ключові частини коду:
buildLinkClass: Функція для динамічного створення класів для кожного елемента списку.
Icons: Використовується для відображення відповідних іконок в кожному елементі меню.


Компонент Header є частиною верхньої панелі додатку і відповідає за рендеринг логотипу, імені користувача та кнопки виходу.

Основні функції та особливості:
Адаптивність:
Використовуються хоки useMedia, щоб визначити, чи є пристрій мобільним або планшетом.
Зміни в стилях (наприклад, для мобільних пристроїв) можуть бути зроблені через клас наприклад headerMobileOpen, 
який додається до елемента header, якщо є відкриті модальні вікна (наприклад, для виходу чи редагування).
Навігація:
NavLink використовується для створення посилання на головну сторінку "/".
Логотип і текст "Money Guard" розміщуються в одному елементі списку для навігації.
Ім'я користувача:
Виводиться ім'я користувача через useSelector з Redux-стану за допомогою селектора selectUser.
Кнопка виходу:
Кнопка з іконкою виходу (Icons) відкриває модальне вікно для підтвердження виходу з додатку, через openLogOutModal.
Для планшетів, текст "Exit" буде додаватися поруч з іконкою кнопки виходу.
Redux:
Вибір з Redux-стану:
selectIsLogOutModalOpen, selectIsEditModalOpen, selectIsAddModalOpen — для перевірки, чи відкриті відповідні модальні вікна.
useDispatch використовується для виклику дії, що відкриває модальне вікно для виходу (кнопка "Exit").
Ключові частини коду:
clsx: Використовується для умовного додавання класу headerMobileOpen до заголовка,
якщо будь-яке з модальних вікон відкрито на мобільному пристрої.
 Ця частина коду умовно додає клас s.headerMobileOpen до елемента, в залежності від кількох умов:
isLogOutModalOpen — перевірка, чи відкрите модальне вікно для виходу.
isEditModalOpen — перевірка, чи відкрите модальне вікно для редагування.
isAddModalOpen — перевірка, чи відкрите модальне вікно для додавання.
isMobile — перевірка, чи пристрій є мобільним (на основі хука useMedia).
Пояснення:
Якщо хоча б одне з трьох модальних вікон відкрито (isLogOutModalOpen, isEditModalOpen, isAddModalOpen) і якщо пристрій є мобільним (isMobile), 
то буде додано клас s.headerMobileOpen до елемента header.  
clsx — це утиліта для умовного додавання класів, яка додає клас тільки в тому випадку, якщо умова є істинною.
Icons: Використовуються для відображення SVG-іконок, таких як логотип і кнопка виходу.


Цей компонент Balance відображає баланс користувача в гривнях (₴) з правильним форматуванням.

Ось що робить цей код:
useSelector(selectUser): Використовується для отримання даних користувача зі стану Redux. У цьому випадку витягується значення балансу користувача за допомогою селектора selectUser.

formattedBalance: Це змінна, яка містить форматований баланс користувача. Для форматування використовуються:

Intl.NumberFormat("uk-UA", {...}): Форматує число в українському стилі (де кома використовується як розділювач тисяч, а крапка — для десяткових знаків).

minimumFractionDigits: 2: Гарантує, що баланс завжди відображається з двома знаками після коми (наприклад, 1000.00).

.replace(",", "."): Заміна коми на крапку, щоб у числі використовувалась точка як розділювач десяткових частин (наприклад, 1,23 на 1.23).

JSX:
Виводиться текст "Your balance" та відформатований баланс користувача у вигляді ₴ {formattedBalance}.
Все це обгорнуто в контейнер з класом s.wrapper, а для текстів використовуються класи s.title та s.balance для стилізації.
Результат:
Компонент відображає баланс користувача в форматі гривень, правильно округлений до двох знаків після коми.



Цей компонент AddButton є кнопкою для відкриття модального вікна для додавання транзакцій або інших дій, залежно від логіки вашої програми.

Ось що робить цей код:
useDispatch: Це хук з бібліотеки react-redux, який дозволяє вам отримати доступ до функції dispatch, щоб відправляти дії у Redux.
onClick={() => { dispatch(openAddModal()); }}: При натисканні на кнопку, викликається дія openAddModal, яка, ймовірно, відкриває модальне вікно для додавання чогось (наприклад, транзакції). Ця дія викликається через dispatch, що змінює стан Redux.
<Icons />: Використовує компонент Icons для рендерингу іконки плюсика. Іконка має клас s.plusIcon для стилізації і задані розміри width={20} і height={20}.
Клас CSS s.addBtn: Клас для стилізації самої кнопки, де можна задавати вигляд кнопки, наприклад, фон, розмір, розташування і т.д.

Підсумок:
Компонент AddButton створює кнопку з іконкою плюсика, при натисканні на яку відкривається модальне вікно для додавання нового елемента (наприклад, транзакції).



Компонент Chart відповідає за відображення кругової діаграми (Doughnut chart), яка показує витрати по різних категоріях, зібраних із стану Redux. Ось детальний опис того, що робить цей код:

1. Імпорти:
Doughnut: Це компонент із бібліотеки react-chartjs-2, який відображає кругову діаграму.
ChartJS: Бібліотека Chart.js для створення графіків. Використовуються елементи ArcElement, Tooltip та Legend для роботи з діаграмами.
selectPeriodTransactions: Селектор для отримання даних про транзакції за певний період із Redux.

2. Отримання даних із Redux:
Використовується хук useSelector для отримання даних про транзакції за період з Redux через селектор selectPeriodTransactions.
3. Обробка даних:
categoriesSummary: Це масив категорій і їх підсумків, з якого фільтруються тільки категорії типу EXPENSE (витрати).
Якщо не знайдено жодної категорії витрат, повертається повідомлення з картинкою, що транзакцій немає.
4. Створення даних для діаграми:
data: Це об'єкт, що передається в компонент Doughnut для побудови діаграми. Він містить:
labels: Назви категорій витрат.
datasets: Масив, що містить дані для діаграми (суми витрат по кожній категорії), а також кольори для відображення ділянки діаграми.
5. Відображення діаграми:
Якщо є витрати, відображається Doughnut діаграма з отриманими даними.
В центрі діаграми відображається periodTotal — загальна сума витрат за обраний період.
6. Якщо транзакцій немає:
Якщо немає транзакцій типу EXPENSE, відображається зображення statisticsNoTransactions з повідомленням "No Transactions Yet".
7. Стилізація:
Використовуються стилі з s (імпортовані з Chart.module.css), щоб налаштувати вигляд діаграми та інших елементів (наприклад, контейнера, тексту в центрі діаграми).
Підсумок:
Компонент Chart відповідає за відображення кругової діаграми витрат на основі категорій, отриманих із Redux. Якщо витрат немає, він показує зображення із повідомленням, що немає транзакцій.


Компонент StatisticsDashboard дозволяє користувачам вибирати місяць і рік для перегляду статистики за певний період. Ось детальний опис кроків, які відбуваються в цьому коді:

1. Імпорти:
useDispatch: Хук з react-redux, який дозволяє викликати дії Redux.
Select: Компонент з бібліотеки react-select для відображення селектів (вибір місяця та року).
useEffect та useState: Хуки з React для управління станом і виконання побічних ефектів.
getPeriodTransactions: Операція для отримання транзакцій за певний період з Redux.
 

const allYear = new Date().getFullYear();
const years = Array.from({ length: 10 }, (_, i) => allYear - i);
export const yearOption = years.map((year) => ({
  value: year,
  label: year.toString(),
}));

export const month = [
  { value: "AllMonth", label: "All month" },
  { value: 1, label: "January" },
  { value: 2, label: "February" },
  { value: 3, label: "March" },
  { value: 4, label: "April" },
  { value: 5, label: "May" },
  { value: 6, label: "June" },
  { value: 7, label: "July" },
  { value: 8, label: "August" },
  { value: 9, label: "September" },
  { value: 10, label: "October" },
  { value: 11, label: "November" },
  { value: 12, label: "December" },
];
  const [selectDate, setSelectDate] = useState({
    year: new Date().getFullYear(),
  });

 Функції для оновлення стану:
handleDateMonth: Ця функція викликається при виборі місяця. Якщо вибрано "AllMonth" (усі місяці), стан оновлюється лише для року, без зміни місяця. Якщо вибрано конкретний місяць, то оновлюється значення місяця в стані.

handleDateYear: Ця функція викликається при виборі року. Вона оновлює стан, змінюючи рік.
Виконання операції для отримання транзакцій:
useEffect: Використовується для виклику операції getPeriodTransactions, яка отримує транзакції за період, коли змінюється стан selectDate. Це дозволяє динамічно оновлювати транзакції для вибраного місяця та року.
UI компоненти:
Select (місяць): Вибір місяця з масиву month. Якщо обрано "AllMonth", стан selectDate оновлюється тільки з роком.
Select (рік): Вибір року з масиву yearOption. Значення для року вибирається з масиву, в якому значення року відповідає selectDate.year.
Стилізація:
Використовуються стилі з s.boxSelect та s.select для налаштування вигляду селектів. Клас classNamePrefix="react-select" додає префікс для CSS-класів, генерованих бібліотекою react-select.
Підсумок:
Компонент StatisticsDashboard дозволяє вибирати місяць і рік для перегляду статистики, а потім запитує відповідні транзакції через Redux. Вибір місяця та року оновлює стан і викликає операцію для отримання нових даних.


StatisticsTable
Створити компонент таблиці, яка відображає витрати за категоріями для конкретного періоду. Також потрібно відобразити підсумки витрат та доходів.

Опис компонента:
Джерело даних:

Дані отримуються з Redux store через useSelector, а саме з selectPeriodTransactions.
Компонент має працювати з такими даними:
categoriesSummary: масив категорій з витратами та доходами.
incomeSummary: підсумок доходів.
expenseSummary: підсумок витрат.
Фільтрація даних:
Тільки категорії витрат (type === "EXPENSE") повинні відображатися в таблиці.
Таблиця:
Заголовок таблиці:
Перша колонка: Category (Категорія).
Друга колонка: Sum (Сума).
Кожен рядок таблиці містить:
Назву категорії.
Суму витрат за цією категорією.
Внизу таблиці відображаються два підсумкові рядки:

Підсумок витрат (expenseSummary), з відображенням суми витрат.

Підсумок доходів (incomeSummary), з відображенням суми доходів.
Підсумкові суми повинні бути відформатовані до двох знаків після коми.



Компонент TransactionsList відображає список транзакцій або повідомлення про відсутність транзакцій. Ось опис його роботи:

Опис:
Отримання даних з Redux:
Компонент використовує хук useSelector, щоб отримати список транзакцій із Redux store за допомогою селектора selectTransactions.

Сортування транзакцій:
Транзакції сортуються за датою (від найновіших до найстаріших) за допомогою методу .sort(), який порівнює дати транзакцій.
Медіа-запит для адаптивності:
Використовується хук useMedia, щоб перевірити, чи є пристрій мобільним (через isMobile), для адаптації відображення таблиці.
Відображення "Немає транзакцій":
Якщо список транзакцій порожній (transactions.length === 0), відображається зображення про відсутність транзакцій за допомогою noTransactionImage.
Відображення таблиці транзакцій:
Якщо є транзакції, то вони відображаються в таблиці з такими стовпцями:
Date — дата транзакції.
Type — тип транзакції.
Category — категорія транзакції.
Comment — коментар до транзакції.
Sum — сума транзакції.
Actions — кнопки для взаємодії з транзакцією (наприклад, редагування чи видалення).
Для мобільних пристроїв таблиця не відображає заголовки таблиці.
**Компонент TransactionsItem:
Кожен рядок таблиці відображається через компонент TransactionsItem, в який передається окрема транзакція.
Структура:
TransactionsList:
Основний контейнер для списку транзакцій або повідомлення про їх відсутність.
Відповідає за сортування транзакцій і їх відображення в таблиці.
Використовує адаптивне відображення залежно від типу пристрою (мобільний або десктоп).
TransactionsItem:
Компонент, який відповідає за відображення окремої транзакції в таблиці.
CSS Модулі:
Використовуються для стилізації таблиці та інших елементів компонента.
   

Опис компонента TransactionsItem
Цей компонент відповідає за відображення кожної окремої транзакції в таблиці. В залежності від розміру екрану (мобільний чи десктопний), форматування та виведення даних змінюється.

Опис роботи компонента:
Приймає пропси:
transaction: об'єкт транзакції, який містить усі дані, необхідні для відображення.
Форматування суми та дати:
sum — абсолютне значення суми транзакції (для відображення без знака мінус).
formSum — форматована сума, яка використовується для відображення у вигляді числа з розділенням на тисячі.
formatDate — функція, яка форматує дату транзакції в форматі "dd.mm.yy".
Отримання категорії транзакції:
Використовує селектор selectCategories для отримання списку категорій і допоміжну функцію getTransactionCategory, щоб визначити, яка категорія відповідає поточній транзакції.
Хук useDispatch:
handleClick — функція для відкриття модального вікна редагування транзакції, що передає поточну транзакцію в Redux за допомогою дії openEditModal і встановлення поточної транзакції через setCurrentTransaction.
Адаптивне відображення:
Використовує хук useMedia, щоб перевірити, чи є екран мобільним (через змінну isMobile).
На мобільних пристроях відображається спрощена версія з великими текстами та більшими кнопками. На десктопі — таблиця з усіма стовпцями.
Видалення транзакції:
Кнопка "Delete" викликає дію deleteTransaction, яка відправляє запит на видалення транзакції з Redux.
Рендеринг:
Для десктопу:
Відображається таблиця з усіма стовпцями: дата, тип, категорія, коментар, сума, і кнопки для редагування та видалення.
Для мобільних пристроїв:
Відображається спрощена версія транзакції, де кожен елемент (дата, тип, категорія, коментар, сума) супроводжується підписами, і кнопки мають більше місця для натискання.
Опис елементів:
Кнопка редагування — відкриває модальне вікно для редагування транзакції.
Кнопка видалення — викликає дію для видалення транзакції.

Ітак, TransactionsItem рендерить:
Табличний рядок (<tr>) для транзакції.

Іконку для редагування транзакції.

Кнопку "Edit" та кнопку "Delete".
  const sum = Math.abs(transaction.amount);
  const formSum = new Intl.NumberFormat().format(sum);

const formatDate = (dateString) => {
  const options = { day: "2-digit", month: "2-digit", year: "2-digit" };
  return new Date(dateString).toLocaleDateString("uk-UA", options);
};
